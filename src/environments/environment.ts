export const environment = {
  production: false,
  otaServerUrl: 'http://localhost:3000',
  versionCheckIntervalMs: 5 * 60 * 1000,
  maxCachedBundles: 3,
  bundleHealthCheckTimeoutMs: 15_000,
  maxRetries: 3,
};
